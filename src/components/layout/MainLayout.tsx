import type { ChildrenProp } from "@/types/children"
import Header from "@/components/ui/Header"

const MainLayout = ({children}: ChildrenProp) => {
  return (
   <>
   <Header />
   </>
  )
}

export default MainLayout